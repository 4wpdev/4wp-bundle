{
  "name": "forwp-bundle",
  "version": "1.0.3",
  "description": "4WP Bundle plugin build setup",
  "scripts": {
    "build": "wp-scripts build && mkdir -p build/addons/show-more && cp addons/show-more/block.json build/addons/show-more/block.json",
    "start": "wp-scripts start"
  },
  "devDependencies": {
    "@wordpress/scripts": "^26.0.0"
  }
}
